import 'package:flutter/material.dart';

class AppColors {
  // --- ألوان لوحة التحكم (Executive Light Narrative) ---
  static const Color background = Color(0xFFF8FAFC);
  static const Color surface = Color(0xFFFFFFFF);
  static const Color cardBackground = Color(0xFFFFFFFF);
  static const Color primary = Color(0xFF0052D1);          // Blue Primary
  static const Color primaryContainer = Color(0xFFE0EAFF);
  static const Color accent = Color(0xFF0052D1);
  static const Color textPrimary = Color(0xFF131B2E);      // Dark Slate
  static const Color textSecondary = Color(0xFF505F76);
  static const Color outline = Color(0xFF737686);

  // --- ألوان شاشة الترحيب واللوجو (Dark Teal & Navy Splash) ---
  static const Color splashBackground = Color(0xFF0B1326); // Deep Navy Dark
  static const Color brandDark = Color(0xFF1C3D55);        // Navy Logo Base
  static const Color brandTeal = Color(0xFF2EB5B7);        // Vibrant Teal Accent
  static const Color splashSurface = Color(0xFF171F33);

  // --- ألوان حالات الفواتير (Badges) ---
  static const Color paidGreen = Color(0xFF10B981);
  static const Color paidGreenBg = Color(0xFFD1FAE5);
  static const Color unpaidRed = Color(0xFFEF4444);
  static const Color unpaidRedBg = Color(0xFFFEE2E2);
  static const Color draftGrey = Color(0xFF64748B);
  static const Color draftGreyBg = Color(0xFFF1F5F9);

  // --- ألوان الطباعة للـ PDF ---
  static const int pdfPrimaryColor = 0xFF0052D1;
  static const int pdfDarkSlate = 0xFF131B2E;
  static const int pdfLightBackground = 0xFFF8FAFC;
}